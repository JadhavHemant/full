// import React from 'react';
// import { useUser } from '../Context/UserContext';

// const Image = ({ className = "w-10 h-10 rounded-full object-cover", fallbackText = "No image" }) => {
//   const user = useUser();

//   if (!user?.image) {
//     return <span>{fallbackText}</span>;
//   }

//   return (
//     <img
//       src={`http://localhost:5351/${user.image}`}
//       alt="User"
//       className={className}
//     />
//   );
// };

// export default Image;
